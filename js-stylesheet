document.addEventListener("mousemove", function(event) {
    var circle = document.querySelector(".sqs-block-code .circle");
    circle.style.left = event.pageX + "px";
    circle.style.top = event.pageY + "px";
});
